# Recommended for deployment reliability. The public Robinhood RPC is the fallback.
ROBINHOOD_RPC_URL=https://rpc.mainnet.chain.robinhood.com

# Required. Use the funded wallet that will own the deployed contracts.
ROBINHOOD_PRIVATE_KEY=

# Optional. Contract verification runs immediately after deployment when true.
# VERIFY_CONTRACTS=true
